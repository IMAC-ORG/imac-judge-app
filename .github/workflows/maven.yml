# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Create Tag

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Get Latest Tag
      id: latest-tag
      run: |
        git fetch --tags
        latest_tag=$(git tag --list 'v*' --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+(\.[0-9]+)?$' | head -n 1)
        if [ -z "$latest_tag" ]; then
          latest_tag="v1.0.0"
        fi
        echo "latest_tag=$latest_tag" >> "$GITHUB_OUTPUT"

    - name: Increment Tag
      id: increment-tag
      run: |
        latest_tag="${{ steps.latest-tag.outputs.latest_tag }}"
        prefix=${latest_tag%.*}
        last_number=${latest_tag##*.}
        new_number=$((last_number + 1))
        new_tag="$prefix.$new_number"
        echo "new_tag=$new_tag" >> "$GITHUB_OUTPUT"
        echo "Incremented tag: $new_tag"

    - name: Create Tag
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git tag -a ${{ steps.increment-tag.outputs.new_tag }} -m "Latest released version"
        git push origin ${{ steps.increment-tag.outputs.new_tag }}
